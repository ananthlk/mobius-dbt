{{ config(enabled=false) }}
-- Replaced by schema test: provider_propensity_score_fl.propensity_score accepted_range(0,100)
-- Singular test disabled to avoid BigQuery correlated-subquery restriction.
select 1 as _noop
